<form method="post" action="{{ path('app_extension_delete', {'id': extension.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extension.id) }}">
    <button class="btn">Delete</button>
</form>
