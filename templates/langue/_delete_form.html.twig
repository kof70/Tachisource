<form method="post" action="{{ path('app_langue_delete', {'id': langue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ langue.id) }}">
    <button class="btn">Delete</button>
</form>
